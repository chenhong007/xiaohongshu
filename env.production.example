# 小红书采集系统 - 生产环境配置
# 域名: xhs.aitop.ink
# 
# 使用方法：复制此文件为 .env.production 并修改配置值
# cp env.production.example .env.production

# ==================== 安全配置 ====================
# Flask 密钥（请使用随机生成的长字符串）
# 生成方式: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=请替换为你的随机密钥-至少64个字符

# Cookie 加密密钥（使用 Fernet 密钥）
# 生成方式: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
COOKIE_ENCRYPTION_KEY=请替换为Fernet生成的密钥

# 管理员 API Key（用于危险操作如清空数据库）
# 生成方式: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_API_KEY=请替换为管理员密钥

# ==================== 其他配置 ====================
# CORS 允许的来源（逗号分隔）
CORS_ORIGINS=http://xhs.aitop.ink,https://xhs.aitop.ink

# 日志级别
LOG_LEVEL=WARNING

